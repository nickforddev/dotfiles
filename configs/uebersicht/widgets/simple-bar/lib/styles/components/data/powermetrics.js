export const powerMetricsStyles = /* css */ `
.powermetrics {
  position: relative;
  color: var(--red);
  overflow: hidden;
}

.powermetrics__icon {
  position: relative;
  height: 16px;
}

.powermetrics__temp-icon {
  position: relative;
  top: -3px;
  left: -3px;
  width: 18px;
  fill: var(--red);
}
`
